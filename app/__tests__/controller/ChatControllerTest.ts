import {describe, test, jest } from '@jest/globals';


describe('Chat Controller should behave as expected', () => {
  
  test('test should check for sth', () => {
     const mock= jest.fn();
  });

});